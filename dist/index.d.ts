type Options = {
    maxLength?: number;
    capitalize?: boolean;
    separator?: string;
    numberLength?: number;
};
export declare function generateUsername(options?: Options): string;
export {};
